{
  "_note": "ISO 4217 code -> Wikimedia Commons filename (without the File: prefix). Overrides the Wikidata P18 image for currencies where the default is a coin, a symbol, or a historical note. Files are validated against the Commons API at build time; a missing file is dropped with a manifest warning, never guessed."
}
